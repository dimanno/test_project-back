module.exports = {
    constants: require('./constant'),
    typeUsers: require('./typeUsers.enum'),
    messageResponse: require('./message.enum'),
    statusCode: require('./statusCode.enum'),
    regEx: require('./regulat.express.enum')
}
